
// This layout file is intentionally blank to create a clean print view
// without the main application shell.

export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      {children}
    </>
  );
}
