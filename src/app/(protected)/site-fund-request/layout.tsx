import SiteFundRequestShell from '@/components/site-fund-request/ModuleLayoutShell';

export default function SiteFundRequestLayout({ children }: { children: React.ReactNode }) {
  return <SiteFundRequestShell>{children}</SiteFundRequestShell>;
}
