'use client';

import { useEffect, useMemo, useState } from 'react';
import { collection, onSnapshot, query, where } from 'firebase/firestore';
import { AlertTriangle, BarChart3, Download, FileSpreadsheet, Loader2, Printer, TrendingUp } from 'lucide-react';
import { db } from '@/lib/firebase';
import { useAuth } from '@/components/auth/AuthProvider';
import { useAuthorization } from '@/hooks/useAuthorization';
import { PaymentObligation, RP_COLLECTIONS, currency, effectiveStatus } from '@/lib/recurring-payments';
import { useToast } from '@/hooks/use-toast';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';

export default function RecurringPaymentReports(){const {user}=useAuth();const {can}=useAuthorization();const {toast}=useToast();const organizationId=user?.organizationId||'default';const [payments,setPayments]=useState<PaymentObligation[]>([]);const [loading,setLoading]=useState(true);const [exporting,setExporting]=useState(false);useEffect(()=>onSnapshot(query(collection(db,RP_COLLECTIONS.payments),where('organizationId','==',organizationId)),snap=>{setPayments(snap.docs.map(d=>({id:d.id,...d.data(),status:effectiveStatus({id:d.id,...d.data()} as PaymentObligation)} as PaymentObligation)));setLoading(false)},()=>setLoading(false)),[organizationId]);const open=useMemo(()=>payments.filter(p=>!['Paid','Closed','Cancelled','Waived'].includes(p.status)),[payments]);const now=new Date();const today=new Date(now.getFullYear(),now.getMonth(),now.getDate());const outflow=(days:number)=>open.filter(p=>{const due=new Date(`${p.dueDate}T00:00:00`);return due>=today&&due<=new Date(today.getTime()+days*86400000)}).reduce((s,p)=>s+(p.billAmount||p.expectedAmount),0);const byCategory=group(payments,p=>p.category);const byVendor=group(payments,p=>p.vendorName);const ageing=[['1–7 days',1,7],['8–15 days',8,15],['16–30 days',16,30],['31–60 days',31,60],['Above 60 days',61,100000]].map(([label,min,max])=>{const subset=open.filter(p=>{const days=Math.floor((today.getTime()-new Date(`${p.dueDate}T00:00:00`).getTime())/86400000);return days>=Number(min)&&days<=Number(max)});return {label:String(label),count:subset.length,amount:subset.reduce((s,p)=>s+(p.billAmount||p.expectedAmount)-(p.settledAmount||p.paidAmount),0)}});const monthly=Array.from({length:6},(_,index)=>{const d=new Date(today.getFullYear(),today.getMonth()-(5-index),1);const key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;const subset=payments.filter(p=>p.dueDate.startsWith(key));return {month:d.toLocaleString('en-IN',{month:'short',year:'2-digit'}),expected:subset.reduce((s,p)=>s+p.expectedAmount,0),actual:subset.reduce((s,p)=>s+(p.billAmount||p.expectedAmount),0),paid:subset.reduce((s,p)=>s+p.paidAmount,0)}});async function excel(){setExporting(true);try{const ExcelJS=(await import('exceljs')).default;const wb=new ExcelJS.Workbook();wb.creator='SEL Live';wb.created=new Date();const register=wb.addWorksheet('Payment Register');register.columns=[['Cycle','cycleKey',22],['Title','title',32],['Category','category',24],['Vendor','vendorName',24],['Due Date','dueDate',14],['Expected','expectedAmount',14],['Bill Amount','billAmount',14],['Paid','paidAmount',14],['Status','status',18],['Stage','stage',22]].map(([header,key,width])=>({header:String(header),key:String(key),width:Number(width)}));payments.forEach(p=>register.addRow({...p,billAmount:p.billAmount||p.expectedAmount}));styleSheet(register);const categories=wb.addWorksheet('Category Summary');categories.columns=[{header:'Category',key:'name',width:30},{header:'Count',key:'count',width:12},{header:'Amount',key:'amount',width:18}];byCategory.forEach(row=>categories.addRow(row));styleSheet(categories);const vendors=wb.addWorksheet('Vendor Summary');vendors.columns=[{header:'Vendor',key:'name',width:30},{header:'Count',key:'count',width:12},{header:'Amount',key:'amount',width:18}];byVendor.forEach(row=>vendors.addRow(row));styleSheet(vendors);const age=wb.addWorksheet('Overdue Ageing');age.columns=[{header:'Bucket',key:'label',width:20},{header:'Count',key:'count',width:12},{header:'Outstanding',key:'amount',width:18}];ageing.forEach(row=>age.addRow(row));styleSheet(age);const buffer=await wb.xlsx.writeBuffer();const blob=new Blob([buffer],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});download(blob,`recurring-payment-report-${new Date().toISOString().slice(0,10)}.xlsx`);toast({title:'Excel report exported'})}catch{toast({title:'Excel export failed',variant:'destructive'})}finally{setExporting(false)}}if(loading)return <div className="flex min-h-[50vh] items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-indigo-600"/></div>;if(!can('View','Recurring Payments.Reports'))return <Card><CardContent className="py-16 text-center"><AlertTriangle className="mx-auto mb-3 h-9 w-9 text-amber-500"/><p className="font-semibold text-muted-foreground">You don&apos;t have permission to view this report.</p></CardContent></Card>;return <div className="space-y-5 print:p-0"><Card className="border-0 bg-gradient-to-r from-violet-700 to-indigo-700 text-white print:bg-white print:text-black"><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-2xl font-bold">Recurring Payment Analytics</h1><p className="text-sm text-violet-100 print:text-slate-600">Cash-flow, category, vendor, trend and overdue analysis</p></div><div className="flex gap-2 print:hidden">{can('Export','Recurring Payments.Reports')&&<Button variant="secondary" onClick={excel} disabled={exporting}>{exporting?<Loader2 className="mr-2 h-4 w-4 animate-spin"/>:<FileSpreadsheet className="mr-2 h-4 w-4"/>}Excel</Button>}<Button variant="secondary" onClick={()=>window.print()}><Printer className="mr-2 h-4 w-4"/>Print / PDF</Button></div></CardContent></Card><div className="grid grid-cols-2 gap-3 lg:grid-cols-5">{[7,15,30,60,90].map(days=><Card key={days}><CardContent className="p-4"><p className="text-xs text-muted-foreground">Next {days} days</p><p className="mt-1 text-xl font-bold">{currency(outflow(days))}</p><p className="text-xs text-muted-foreground">Expected outflow</p></CardContent></Card>)}</div><div className="grid gap-4 lg:grid-cols-2"><SummaryTable title="Category-wise expense" rows={byCategory}/><SummaryTable title="Vendor-wise expense" rows={byVendor.slice(0,12)}/></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><TrendingUp className="h-5 w-5 text-indigo-600"/>Six-month comparison</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Month</TableHead><TableHead className="text-right">Expected</TableHead><TableHead className="text-right">Actual bills</TableHead><TableHead className="text-right">Paid</TableHead><TableHead className="text-right">Variance</TableHead></TableRow></TableHeader><TableBody>{monthly.map(row=><TableRow key={row.month}><TableCell>{row.month}</TableCell><TableCell className="text-right">{currency(row.expected)}</TableCell><TableCell className="text-right">{currency(row.actual)}</TableCell><TableCell className="text-right">{currency(row.paid)}</TableCell><TableCell className={`text-right ${row.actual>row.expected?'text-red-600':'text-emerald-600'}`}>{row.expected?`${(((row.actual-row.expected)/row.expected)*100).toFixed(1)}%`:'—'}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><BarChart3 className="h-5 w-5 text-red-600"/>Overdue ageing</CardTitle><CardDescription>Outstanding obligations grouped by days past due</CardDescription></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Ageing bucket</TableHead><TableHead className="text-right">Payments</TableHead><TableHead className="text-right">Outstanding</TableHead></TableRow></TableHeader><TableBody>{ageing.map(row=><TableRow key={row.label}><TableCell>{row.label}</TableCell><TableCell className="text-right">{row.count}</TableCell><TableCell className="text-right font-semibold">{currency(row.amount)}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div>}
function group(payments:PaymentObligation[],key:(p:PaymentObligation)=>string){return Object.entries(payments.reduce<Record<string,{count:number;amount:number}>>((acc,p)=>{const name=key(p)||'Unspecified';acc[name]??={count:0,amount:0};acc[name].count++;acc[name].amount+=(p.billAmount||p.expectedAmount);return acc},{})).map(([name,value])=>({name,...value})).sort((a,b)=>b.amount-a.amount)}
function SummaryTable({title,rows}:{title:string;rows:Array<{name:string;count:number;amount:number}>}){return <Card><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Name</TableHead><TableHead className="text-right">Records</TableHead><TableHead className="text-right">Amount</TableHead></TableRow></TableHeader><TableBody>{rows.map(row=><TableRow key={row.name}><TableCell>{row.name}</TableCell><TableCell className="text-right">{row.count}</TableCell><TableCell className="text-right font-semibold">{currency(row.amount)}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card>}
function styleSheet(sheet:{getRow:(row:number)=>{font:unknown;fill:unknown};views:unknown[];autoFilter?:unknown;rowCount:number;columnCount:number}){sheet.getRow(1).font={bold:true,color:{argb:'FFFFFFFF'}};sheet.getRow(1).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF4F46E5'}};sheet.views=[{state:'frozen',ySplit:1}];sheet.autoFilter={from:{row:1,column:1},to:{row:sheet.rowCount,column:sheet.columnCount}}}
function download(blob:Blob,name:string){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();URL.revokeObjectURL(a.href)}
