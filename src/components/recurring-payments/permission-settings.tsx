'use client';
import Link from 'next/link';import { KeyRound,ShieldCheck,Users } from 'lucide-react';import { useAuthorization } from '@/hooks/useAuthorization';import { permissionModules } from '@/lib/permissions';import { Badge } from '@/components/ui/badge';import { Button } from '@/components/ui/button';import { Card,CardContent,CardDescription,CardHeader,CardTitle } from '@/components/ui/card';
const roleGuidance=[['Super Admin','All organizations, global settings and permission administration'],['Organization Admin','Organization masters, rules, reports and all organization payments'],['Payment Owner','Bills, bill details, queries and submission'],['Verifier','Bill verification, correction and rejection'],['Approver','Assigned approval actions and delegation'],['Accounts User','Payment transactions, proof and closure'],['Auditor','Read-only records, documents and audit trails'],['Viewer','Permitted read-only pages']];
export default function RecurringPermissionSettings(){const {can}=useAuthorization();const modulePermissions=permissionModules['Recurring Payments'];const canManage=can('Manage Permissions','Recurring Payments.Settings');return <div className="space-y-5"><Card className="border-0 bg-gradient-to-r from-slate-950 to-indigo-950 text-white"><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-2xl font-bold">Recurring Payment Permissions</h1><p className="text-sm text-indigo-100">Role-based access for masters, bills, approvals, transactions, reports and configuration</p></div>{canManage&&<Link href="/settings/role-management"><Button className="bg-white text-indigo-800 hover:bg-indigo-50"><KeyRound className="mr-2 h-4 w-4"/>Manage roles</Button></Link>}</CardContent></Card><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-indigo-600"/>Permission groups</CardTitle><CardDescription>These permissions appear in Role Management.</CardDescription></CardHeader><CardContent className="space-y-3">{Object.entries(modulePermissions).map(([group,actions])=><div className="rounded-xl border p-4" key={group}><p className="font-semibold">{group}</p><div className="mt-2 flex flex-wrap gap-1.5">{Array.isArray(actions)?actions.map(action=><Badge variant="outline" key={action}>{action}</Badge>):<Badge variant="outline">Module access</Badge>}</div></div>)}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Users className="h-5 w-5 text-violet-600"/>Recommended role model</CardTitle><CardDescription>Suggested separation of duties for maker-checker control.</CardDescription></CardHeader><CardContent className="space-y-3">{roleGuidance.map(([role,description])=><div className="rounded-xl border p-4" key={role}><p className="font-semibold">{role}</p><p className="text-sm text-muted-foreground">{description}</p></div>)}</CardContent></Card></div></div>}
